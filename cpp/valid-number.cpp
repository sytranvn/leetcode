#include <iostream>
#include <string>
#include "valid-number.h"

using namespace std;

bool solution(string num) {
    

    return false;
}

int main() {
    int n;
    cin >> n;
    while (n--) {
        string s;
        cin >> s;
        cout << (solution(s) ? "true" : "false") << endl;
    }
}

bool is_decimal(string num)
{
    return false;
}

bool is_integer(string num)
{
    return false;
}
